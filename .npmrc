# sharp is pulled in by @xenova/transformers for image pipelines only.
# We use text-only embeddings so the native binary is never needed.
# This prevents sharp's postinstall from downloading libvips.
ignore-scripts=true
